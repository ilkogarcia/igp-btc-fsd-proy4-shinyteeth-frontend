// All functions for different data inputs validation
export const Validation = (type, data) => {
  switch (type) {
    case 'email': {
      // Aquí validamos los datos de tipo email
      break
    }
    case 'password': {
      // Aquí validamos los datos de tipo password
      break
    }
  }
}
